# Snapshot report for `src/transform.spec.ts`

The actual snapshot is saved in `transform.spec.ts.snap`.

Generated by [AVA](https://avajs.dev).

## transforms plain let

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms const

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    const x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms var

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    var x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms annotated number

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x: number = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms Map type annotation

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let m: Map<string, number> = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (new Map()));␊
    __persist.track(__persist_h0, () => $state.snapshot(m), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms nested generic annotation

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x: Map<string, Array<Map<string, number>>> = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (new Map()));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms function type argument

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (() => {}));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms parameterized function type argument

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : ((a) => a.length));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms generic constructor initial

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (new Map<string, number>()));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms as record wrapper

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : ({})) as Record<string, number>;␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms satisfies record wrapper

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : ({})) satisfies Record<string, number>;␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms object type argument

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : ({ a: 1 }));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms union type argument

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (1));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms tuple type argument

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (["a", 1]));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms typed field

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C { #__persist_h0 = __persist.createPersistence("k", undefined);␊
    name: string = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms private modifier

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C { #__persist_h0 = __persist.createPersistence("k", undefined);␊
    private name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms readonly modifier

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C { #__persist_h0 = __persist.createPersistence("k", undefined);␊
    readonly name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms private field

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C { #__persist_h0 = __persist.createPersistence("k", undefined);␊
    #name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.#name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms generic class

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C<T> { #__persist_h0 = __persist.createPersistence("k", undefined);␊
    name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms generic heritage

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C extends B<string> { #__persist_h0 = __persist.createPersistence("k", undefined);␊
    name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms implements

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C implements I { #__persist_h0 = __persist.createPersistence("k", undefined);␊
    name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms call heritage

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C extends mixin(B) { #__persist_h0 = __persist.createPersistence("k", undefined);␊
    name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms default export class

> Snapshot 1

    `import * as __persist_module from "svelte-persistent-runes";␊
    export default class C { #__persist_module_h0 = __persist_module.createPersistence("k", undefined);␊
    name = $state(this.#__persist_module_h0.restore() ? this.#__persist_module_h0.value : ("A"));␊
    #__persist_module_e0 = __persist_module.track(this.#__persist_module_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms class expression

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    const C = class { #__persist_h0 = __persist.createPersistence("k", undefined);␊
    name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); };`

## transforms field after method

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C { method() {} #__persist_h0 = __persist.createPersistence("k", undefined);␊
    name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms field after object field

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C { other = { v: 1 }; #__persist_h0 = __persist.createPersistence("k", undefined);␊
    name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms two classes

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class A { constructor() {} #__persist_h0 = __persist.createPersistence("a", undefined);␊
    name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); } class B { #__persist_h1 = __persist.createPersistence("b", undefined);␊
    age = $state(this.#__persist_h1.restore() ? this.#__persist_h1.value : (2));␊
    #__persist_e1 = __persist.track(this.#__persist_h1, () => $state.snapshot(this.age), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms parameter properties

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C { constructor(public x: number) {} #__persist_h0 = __persist.createPersistence("k", undefined);␊
    name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms default constructor parameter

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C { constructor(x = 2) {} #__persist_h0 = __persist.createPersistence("k", undefined);␊
    name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms derived existing constructor

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C extends B { constructor() { super(); } #__persist_h0 = __persist.createPersistence("k", undefined);␊
    name = $state(this.#__persist_h0.restore() ? this.#__persist_h0.value : ("A"));␊
    #__persist_e0 = __persist.track(this.#__persist_h0, () => $state.snapshot(this.name), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms dollar-prefixed identifier

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let $count = 1; let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms unicode binding

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let café = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(café), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms function body

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    function f() { let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f)); return x; }`

## transforms nested block

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    if (true) { let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms URL before call

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    const url = "https://site.test/x"; let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms comment delimiter string before call

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    const delimiter = "/*"; let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms unrelated strings and templates

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    const str = "$persist(1)"; const tpl = \`$persist(2)\`; let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms template key nested parentheses

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence(\`key-${fn("a)")}\`, undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms regex literal with paren and quote

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (/[)']/.test("a")));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms multiline trailing comma

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms module exported let

> Snapshot 1

    `import * as __persist_module from "svelte-persistent-runes";␊
    let __persist_module_h0;␊
    export let x = $state((__persist_module_h0 = __persist_module.createPersistence("k", undefined)).restore() ? __persist_module_h0.value : (0));␊
    __persist_module.track(__persist_module_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms vite module exported let

> Snapshot 1

    `import * as __persist_module from "svelte-persistent-runes";␊
    let __persist_module_h0;␊
    export let x = $state((__persist_module_h0 = __persist_module.createPersistence("k", undefined)).restore() ? __persist_module_h0.value : (0));␊
    __persist_module.track(__persist_module_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms instance exported const

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    export const x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms as number wrapper

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0)) as number;␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms satisfies number wrapper

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0)) satisfies number;␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms non-null assertion wrapper

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0))!;␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms angle bracket assertion wrapper

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = <number>$state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms occupied alias and handle

> Snapshot 1

    `import * as __persist_1 from "svelte-persistent-runes";␊
    const __persist = 1; const __persist_h0 = 2; class C { #__persist_h0 = 3; #__persist_1_h0 = __persist_1.createPersistence("k", undefined);␊
    x = $state(this.#__persist_1_h0.restore() ? this.#__persist_1_h0.value : (0));␊
    #__persist_1_e0 = __persist_1.track(this.#__persist_1_h0, () => $state.snapshot(this.x), (f) => { $effect(f); }, (f) => $effect.root(f)); }`

## transforms markup occupied alias

> Snapshot 1

    `import * as __persist_2 from "svelte-persistent-runes";␊
    let __persist_2_h0;␊
    let x = $state((__persist_2_h0 = __persist_2.createPersistence("k", undefined)).restore() ? __persist_2_h0.value : (0));␊
    __persist_2.track(__persist_2_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms multiple declarators

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let a = 1, x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0)), b = 2;␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms multiple persisted declarators

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let __persist_h1;␊
    let x = $state((__persist_h0 = __persist.createPersistence("x", undefined)).restore() ? __persist_h0.value : (0)), y = $state((__persist_h1 = __persist.createPersistence("y", undefined)).restore() ? __persist_h1.value : (1));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));␊
    __persist.track(__persist_h1, () => $state.snapshot(y), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms constructor assignment

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C { constructor() { const __persist_h0 = __persist.createPersistence("k", undefined);␊
    this.x = $state(__persist_h0.restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(this.x), (f) => { $effect(f); }, (f) => $effect.root(f)); } }`

## transforms constructor lowered field

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    class C { constructor() { const __persist_h0 = __persist.createPersistence("k", undefined);␊
    this.x = $state(__persist_h0.restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(this.x), (f) => { $effect(f); }, (f) => $effect.root(f)); this.y = 1; } }`

## transforms module markup snippet export

> Snapshot 1

    `import * as __persist_module from "svelte-persistent-runes";␊
    export { snippet }; let __persist_module_h0;␊
    let x = $state((__persist_module_h0 = __persist_module.createPersistence("k", undefined)).restore() ? __persist_module_h0.value : (0));␊
    __persist_module.track(__persist_module_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms module alias

> Snapshot 1

    `import * as __persist_module from "svelte-persistent-runes";␊
    let __persist_module_h0;␊
    let x = $state((__persist_module_h0 = __persist_module.createPersistence("k", undefined)).restore() ? __persist_module_h0.value : (0));␊
    __persist_module.track(__persist_module_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms explicit options

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", { storageRead: () => "1" })).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f));`

## transforms switch case

> Snapshot 1

    `import * as __persist from "svelte-persistent-runes";␊
    switch (1) { case 1: let __persist_h0;␊
    let x = $state((__persist_h0 = __persist.createPersistence("k", undefined)).restore() ? __persist_h0.value : (0));␊
    __persist.track(__persist_h0, () => $state.snapshot(x), (f) => { $effect(f); }, (f) => $effect.root(f)); break; }`
